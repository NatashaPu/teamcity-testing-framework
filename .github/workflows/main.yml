name: TeamCity deployment

on: push
jobs:
  #Собирается бекенд и деплоится на стейдж/review app
  backend:
    uses: NatashaPu/teamcity-testing-framework/.github/workflows/backend-stage.yml

  #Собирается фронт и деплоится на стейдж/review app
  frontend:
    uses: NatashaPu/teamcity-testing-framework/.github/workflows/frontend-stage.yml

  #Прогоняются API тесты
  automation-api:
    needs: [ backend, frontend ]
    uses: NatashaPu/teamcity-testing-framework/.github/workflows/automation.yml
    with:
      package: api

  #Прогоняются UI тесты
  automation-ui:
    needs: [ backend, frontend ]
    uses: NatashaPu/teamcity-testing-framework/.github/workflows/automation.yml
    with:
      package: ui

